export class Seno {
    constructor(valor) {
        this.valor = valor;
        this.valor = valor;
    }
    evaluar() {
        return Math.sin(this.valor);
    }
    dibujarFuncion(canvas, contexto, color = 'black', anchoLinea = 1) {
    }
}
//# sourceMappingURL=seno.js.map